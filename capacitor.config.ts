import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-harry-potter',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
